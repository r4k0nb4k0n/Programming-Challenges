#include <cstdio>
#include <vector>

using namespace std;

int T; // 테스트 케이스의 개수.
vector<pair<int,int> > score;

int memo[100001][2];

int solve(int num,int up_down)
{
	// 기저조건.
	if()
}

int main()
{
	int n;
	scanf("%d",&T);

	while(T--)
	{
		scanf("%d",&n);
		score.resize(n);
		for(int i=0;i<n;i++)
		{
			scanf("%d",&v[0].first);
		}
		for(int i=0;i<n;i++)
		{
			scanf("%d",&v[0].second);
		}

		solve
	}
}