#include<stdio.h>#include<math.h>int main(){ int i, j; // i는 소수 j는 소수 판정할 나누는 수 int num = 4; // 4부터 2씩 증가시키면서 테스트  int k; // 골드바흐의 반례 출현시 1 int a; // i가 j로 나누어 떨어질 시 1 int b; //num-i가 j로 나누어 떨어질 시 1 int m; // 확인할 수의 최대값 int n = 1; // 확인할 수의 최소값 int c = 0; // 출력했는지 여부 while(n%2 == 1) {  printf("골드바흐 추론의 반례를 찾을 범위의 최소값을 짝수로 입력하십시오.");  scanf("%d",&n); } printf("최대값을 입력하십시오."); scanf("%d",&m); num = n; for(k = 0;k == 0 && num<=m;num=num+2) {  for(i = 2;i<=num/2;i++)  {   a = 0;   for(j = 2;j<=sqrt(i);j++)   {    if(i == 2 && j == 2)    {    }    else if(i%j == 0)    {     a = 1;    }   }   if(a == 0)   {    b = 0;    for(j = 2;j<=sqrt(num-i);j++)    {     if(num-i == 2 && j == 2)     {     }     else if((num-i)%j == 0)     {      b = 1;     }    }    if(b == 0)    {     printf("%d = %d + %d\n",num,i,num-i);     c = 1;     i = num;     j = i;    }   }  }  if(c == 1)  {   c = 0;  }  else  {   k = 1;   printf("error!");  } } return 0;}
